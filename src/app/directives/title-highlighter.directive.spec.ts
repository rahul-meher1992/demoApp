import { TitleHighlighterDirective } from './title-highlighter.directive';

describe('TitleHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
